services:
  backend:
    image: ${ECR_URI}:${BACKEND_TAG}
    container_name: taskforge-backend
    ports:
      - "80:8000"
    restart: unless-stopped

  frontend:
    image: ${ECR_URI}:${FRONTEND_TAG}
    container_name: taskforge-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost
    restart: unless-stopped
